## 자바 입출력



#### 파일 클래스



OS에 따른 경로 구분자

자바는 플랫폼 독립적 동작을 지향하므로, 경로 구분자를 /를 이용해도 문제 없이 인식함

단 (자바 내 문자열 인식을 위해서는 \\\\ 사용 escape 문자!)

windows만 linux, macos와 달리 역슬래쉬를 사용함



File 클래스는 특정 디렉토리에 파일 객체를 생성하는 기능만 함

파일 '내용'을 쓰거나 읽는 기능은 없음

파일 메타데이터를 보거나, 디렉토리 만들고 삭제





file.listFiles()

file.isFile()



try-with-resources문

-> close 자동으로 해줌

-> 예외처리 해야하는 문장을 try 옆 괄호에 넣어줌



**FileOutputStream**

try(FileOutputStream fos = new FileOutputStream(file,true))



true -> 추가모드 on

=> 덮어쓰기 (새로 만들기)의 반대

=> 옆에 추가!



단점: 성능이 안좋아~

실무에서는 **buffer**를 끼워서 씀

###### 중간 메모리 버퍼 쌓아두고 **"한번에"** 내보낸다!!

입출력 = 속도가 매우 느린편

\*\*\* 버퍼 \*\*\*\*에 가득 싣어서 한번에 내보냄~~~~

실무에서 씀~~~



< 하는 방법>
BufferedOutputStream에 인자로 FileOutputStream을 new해서 넣음!

